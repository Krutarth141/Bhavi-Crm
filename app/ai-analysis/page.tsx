import LegacySurfaceScreen from '@/components/screens/LegacySurfaceScreen';

export default function AIAnalysisPage() {
    return <LegacySurfaceScreen eyebrow="AI Agent" title="AI Analysis" subtitle="The analysis surface remains available for admin review and AI-assisted workflows." points={['Track AI outputs and insights in one place.', 'Keep it tied to the same authenticated session.', 'Mirror the legacy route entry point.']} />;
}