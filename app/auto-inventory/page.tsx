import LegacySurfaceScreen from '@/components/screens/LegacySurfaceScreen';

export default function AutoInventoryPage() {
    return <LegacySurfaceScreen eyebrow="Automation" title="Auto Inventory" subtitle="Automated inventory surfacing remains available from a dedicated route." points={['Track inventory suggestions and automated stock signals.', 'Keep the route visible to admins only.', 'Preserve the original automation entry point.']} />;
}