import LegacySurfaceScreen from '@/components/screens/LegacySurfaceScreen';

export default function FaultFinderPage() {
    return <LegacySurfaceScreen eyebrow="Tools" title="Fault Finder" subtitle="The troubleshooting entry point is preserved as a standalone tools page." points={['Help agents narrow down common device issues.', 'Keep the diagnostic route under CRM auth.', 'Mirror the legacy utility entry point.']} />;
}