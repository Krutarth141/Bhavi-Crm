import LegacySurfaceScreen from '@/components/screens/LegacySurfaceScreen';

export default function EngineerUpdatePage() {
    return <LegacySurfaceScreen eyebrow="Workflow" title="Engineer Update" subtitle="Engineer status updates remain exposed as a dedicated screen." points={['Capture technician progress and notes.', 'Keep the workflow available after login.', 'Mirror the legacy action surface.']} />;
}