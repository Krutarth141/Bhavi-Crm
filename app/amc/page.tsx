import LegacySurfaceScreen from '@/components/screens/LegacySurfaceScreen';

export default function AmcPage() {
    return <LegacySurfaceScreen eyebrow="Service" title="AMC" subtitle="AMC requests and tracking are available as a dedicated route." points={['Review maintenance contracts and renewal cadence.', 'Link AMC bookings to ticket records.', 'Keep the CRM’s service workflow intact.']} />;
}