import LegacySurfaceScreen from '@/components/screens/LegacySurfaceScreen';

export default function AIAgentPage() {
    return <LegacySurfaceScreen eyebrow="AI Agent" title="Virtual AI Agent" subtitle="The legacy AI assistant entry point is now surfaced as a route in the Next.js app." points={['Reserve a place for the agent workflow and prompt surfaces.', 'Keep it behind authenticated access.', 'Align with the current dashboard shell.']} />;
}